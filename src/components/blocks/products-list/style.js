import styled from 'styled-components';

export const ProductsList = styled.ul`
  display: flex;
  flex-direction: column;
  margin: 0;
  padding: 0;
`;

export const ProductsElement = styled.li`
  position: relative;

  margin: 0;
  padding: 0;

  list-style-type: none;
`;
